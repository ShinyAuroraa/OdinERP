apiVersion: v1
kind: Service
metadata:
  name: wms-web
  namespace: wms
  labels:
    app: wms-web
spec:
  type: ClusterIP
  selector:
    app: wms-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
