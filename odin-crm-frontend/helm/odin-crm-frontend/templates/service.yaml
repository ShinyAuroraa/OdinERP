apiVersion: v1
kind: Service
metadata:
  name: {{ include "odin-crm-frontend.fullname" . }}
  labels:
    {{- include "odin-crm-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odin-crm-frontend.selectorLabels" . | nindent 4 }}
