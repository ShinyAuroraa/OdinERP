apiVersion: v1
kind: Service
metadata:
  name: {{ include "odin-crm-backend.fullname" . }}
  labels:
    {{- include "odin-crm-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odin-crm-backend.selectorLabels" . | nindent 4 }}
