---
epicNum: 3
storyNum: 2
epicId: EPIC-WMS-001
storyId: STORY-WMS-3.2
title: "Putaway (Alocação Inteligente de Estoque)"
status: Draft
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [algorithm_review, api_contract_review, performance_test, multi_tenant_test]
createdBy: "@sm (River)"
createdAt: "2026-02-22"
---

# Story 3.2: Putaway (Alocação Inteligente de Estoque)

## Status

Draft

## Story

**As a** operador de armazém (WMS Operator),
**I want** receber sugestões automáticas de localização de armazenagem para cada `StockItem` criado na doca de recebimento — aplicando FIFO para produtos sem controle de validade e FEFO para perecíveis — e confirmar ou sobrescrever a localização via API,
**so that** cada mercadoria recebida seja alocada na posição correta do armazém com rastreabilidade completa (movimento `PUTAWAY` registrado), liberando a doca de recebimento para novos recebimentos e habilitando o fluxo de picking (Story 5.1).

## Complexity

**Estimativa:** L (Large) · ~4-6 dias de desenvolvimento
[Fonte: EPIC-WMS-001 — motor de regras FIFO/FEFO + algoritmo de sugestão de localização + nova entidade PutawayTask + integração com StockItem/StockMovement/AuditLog]

## Dependencies

**Pré-requisitos para iniciar esta story:**
- Story 3.1 concluída ✅ — `ReceivingNote` com `COMPLETED`/`COMPLETED_WITH_DIVERGENCE`, `StockItem` criado em `location.type = RECEIVING_DOCK`, `StockMovement(INBOUND)` disponível, padrão Service/Controller/DTO/Test estabelecido

**Stories desbloqueadas após esta:**
- Story 4.1 — Controle de Estoque em Tempo Real (StockItems em STORAGE prontos para consulta de saldo)
- Story 5.1 — Picking (consome StockItems em STORAGE/PICKING locations)

## Out of Scope

> Itens explicitamente **não implementados** nesta story:

- ❌ Compatibilidade de temperatura por zona (DRY→zona seca, REFRIGERATED→câmara fria) — Zone.storageType não existe; enhancement futuro (Story 4.x)
- ❌ Interface Android para confirmação via scanner — Wave 8 (Story 8.2)
- ❌ Putaway automático sem interação do operador — enhancement futuro com heurísticas ML
- ❌ Regras por tenant customizáveis (configuração de estratégia putaway por armazém) — enhancement futuro (Story 4.x)
- ❌ Putaway para itens `FLAGGED` (status COMPLETED_WITH_DIVERGENCE) — esses itens aguardam Story 3.3 (Quarentena)
- ❌ Algoritmo de rota otimizado por corredor (S-shape/Z-shape) — Story 5.1 (Picking)
- ❌ Reabastecimento automático de zona de picking a partir de bulk — Story 4.5 (Transferências)

## Business Value

**Por que esta story é crítica na Wave 3:**

- **Libera a doca de recebimento:** Enquanto StockItems ficam na `RECEIVING_DOCK`, novas entregas não podem ocupar o espaço. O putaway move os itens para o armazenamento definitivo.
- **Rastreabilidade completa:** O `StockMovement(PUTAWAY)` registra a cadeia INBOUND→PUTAWAY, permitindo auditar onde cada lote/série foi alocado desde o momento do recebimento.
- **Otimização de ocupação:** FIFO/FEFO garante que os itens mais antigos (ou de validade mais próxima) sejam alocados com prioridade, evitando itens presos em dock e vencimento de mercadorias.
- **Pré-requisito de picking:** Nenhum picking pode ocorrer sem que os StockItems estejam em locations do tipo `STORAGE` ou `PICKING`.

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| Putaway gerado duas vezes para mesma ReceivingNote (duplicata) | Média | Médio | `existsByTenantIdAndReceivingNoteId` — se já existe, retornar 409 Conflict |
| Nenhuma location STORAGE disponível no warehouse | Média | Alto | Retornar 422 com mensagem clara; operator pode adicionar localizações antes de tentar novamente |
| Concorrência: dois operadores confirmando o mesmo PutawayTask | Baixa | Médio | `@Version` em `StockItem` já previne double-move via optimistic locking; PutawayTask status check em `@Transactional` |
| StockItem já movido de RECEIVING_DOCK por outro processo | Baixa | Alto | Validar `stockItem.location.type == RECEIVING_DOCK` antes de gerar tarefas; 422 se já movido |
| Confirmar location de outro warehouse (cross-tenant ou cross-warehouse) | Média | Alto | Validar que `confirmedLocation.shelf.aisle.zone.warehouse == sourceLocation.shelf.aisle.zone.warehouse` e tenant |

---

## Acceptance Criteria

1. Dado uma `ReceivingNote` com status `COMPLETED` ou `COMPLETED_WITH_DIVERGENCE`, ao chamar `POST /api/v1/receiving-notes/{id}/putaway-tasks`, o sistema cria um `PutawayTask` com status `PENDING` para cada `ReceivingNoteItem` cujo `stockItem.location.type == RECEIVING_DOCK`, retornando `201 Created` com a lista de tasks geradas.

2. Para produtos com `product.controlsExpiry = true`, a `suggestedLocation` é determinada pelo algoritmo FEFO: entre as locations disponíveis do warehouse, prefere a que já contém o mesmo produto com o lote de validade mais próxima (`lot.expiryDate ASC`); se não houver, seleciona qualquer location disponível.

3. Para produtos com `product.controlsExpiry = false`, a `suggestedLocation` é determinada pelo algoritmo FIFO: a location é sugerida baseada na disponibilidade de capacidade, priorizando co-localização com mesmo produto; o `stockItem.receivedAt ASC` é registrado como chave do algoritmo aplicado.

4. Ao chamar `POST /api/v1/putaway-tasks/{id}/start`, o status da task muda de `PENDING` para `IN_PROGRESS`; retorna 200 com task atualizada.

5. Ao chamar `POST /api/v1/putaway-tasks/{id}/confirm` com `{ "confirmedLocationId": "<uuid>" }`:
   - O sistema valida que a location pertence ao mesmo warehouse e tenant, tem `type IN (STORAGE, PICKING)` e está `active = true`
   - O sistema valida que a capacidade da location permite o item (`capacityUnits IS NULL OR countOfItemsInLocation < capacityUnits`)
   - O `stockItem.location` é atualizado para `confirmedLocation`
   - Um `StockMovement(type=PUTAWAY, sourceLocation=dockLocation, destinationLocation=confirmedLocation)` é criado
   - Um `AuditLog(action=MOVEMENT, entityType="PutawayTask")` é registrado com o operador e a location de destino
   - O `PutawayTask.status` muda para `CONFIRMED`, `confirmedLocation` é preenchido
   - Retorna 200 com task atualizada

6. Ao chamar `POST /api/v1/putaway-tasks/{id}/cancel`, o status muda para `CANCELLED`, o `StockItem` permanece na doca (nenhum movimento criado); retorna 200. Requer role `WMS_SUPERVISOR` ou `WMS_ADMIN`.

7. Se a `ReceivingNote` não estiver em status `COMPLETED` ou `COMPLETED_WITH_DIVERGENCE`, `POST /api/v1/receiving-notes/{id}/putaway-tasks` retorna `422 Unprocessable Entity` com mensagem de erro descritiva.

8. Se já existirem `PutawayTask`s geradas para a mesma `ReceivingNote`, o endpoint retorna `409 Conflict` (idempotência por `receivingNoteId`).

9. Se a `confirmedLocationId` na request de `confirm` não pertencer ao mesmo warehouse/tenant, não tiver tipo compatível ou não tiver capacidade disponível, o endpoint retorna `422` com mensagem específica.

10. `GET /api/v1/putaway-tasks` lista todas as tasks do tenant com filtros opcionais por `status` e `warehouseId`; `GET /api/v1/putaway-tasks/{id}` retorna uma task pelo ID.

11. Todos os endpoints aplicam isolamento multi-tenant via `TenantContextHolder.getTenantId()`; tentativa de acesso a task de outro tenant retorna `404 Not Found`.

12. Roles exigidas: `generate`, `start`, `confirm` requerem `WMS_OPERATOR`, `WMS_SUPERVISOR` ou `WMS_ADMIN`; `cancel` requer `WMS_SUPERVISOR` ou `WMS_ADMIN`.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: API (service logic, endpoints, business rules)
**Secondary Type(s)**: Database (nova entidade PutawayTask + migration V5), Architecture (motor de regras FIFO/FEFO)
**Complexity**: High — novo domínio com algoritmo de seleção, múltiplas entidades afetadas (StockItem, StockMovement, AuditLog), performance crítica na sugestão de localização

### Specialized Agent Assignment

**Primary Agents**:
- @dev (pré-commit reviews, implementação)
- @architect (algorithm_review — FIFO/FEFO correctness, performance_test — N+1 no algoritmo)

**Supporting Agents**:
- @qa (validação das regras FIFO/FEFO, edge cases de capacidade, isolamento multi-tenant)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` antes de marcar story completa
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main` antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only (registrado em Dev Notes)

### CodeRabbit Focus Areas

**Primary Focus**:
- N+1 queries no algoritmo de sugestão de localização (Location → Shelf → Aisle → Zone → Warehouse chain)
- Isolamento multi-tenant em todas as queries (tenant_id em todos os filtros)
- Optimistic locking em StockItem.location update (verificar @Version)
- Validação de capacidade com PESSIMISTIC_WRITE lock para evitar race condition

**Secondary Focus**:
- Idempotência na geração de tasks (`existsByTenantIdAndReceivingNoteId`)
- ReferenceType.PUTAWAY_TASK adicionado ao constraint do banco antes de uso
- Reversibilidade: cancel não cria movimento, apenas marca status

---

## Tasks / Subtasks

- [ ] Task 1 — Migration V5: tabela `putaway_tasks` e atualização de constraints (AC: 1, 5, 8)
  - [ ] 1.1 Criar `V5__putaway_tasks.sql` com tabela `putaway_tasks` (id, tenant_id, receiving_note_id, stock_item_id, source_location_id, suggested_location_id, confirmed_location_id, status, strategy_used, created_at, updated_at, version)
  - [ ] 1.2 Adicionar constraints: FK para receiving_notes, stock_items, locations; CHK status (`PENDING`, `IN_PROGRESS`, `CONFIRMED`, `CANCELLED`); CHK strategy_used (`FIFO`, `FEFO`)
  - [ ] 1.3 Atualizar constraint `chk_stock_movements_reference_type` (já modificada em V4): adicionar `PUTAWAY_TASK` ao CHECK
  - [ ] 1.4 Criar índices: `idx_putaway_tasks_tenant_status`, `idx_putaway_tasks_receiving_note`, `idx_putaway_tasks_stock_item`
  - [ ] 1.5 Adicionar seção ROLLBACK comentada no final do arquivo

- [ ] Task 2 — Enums e entidade `PutawayTask` (AC: 1, 4, 6)
  - [ ] 2.1 Criar `PutawayStatus` enum: `PENDING, IN_PROGRESS, CONFIRMED, CANCELLED` em `com.odin.wms.domain.enums`
  - [ ] 2.2 Criar `PutawayStrategy` enum: `FIFO, FEFO` em `com.odin.wms.domain.enums`
  - [ ] 2.3 Adicionar `PUTAWAY_TASK` ao enum `ReferenceType` existente
  - [ ] 2.4 Criar entity `PutawayTask extends BaseEntity` em `com.odin.wms.domain.entity` com campos mapeados para V5
  - [ ] 2.5 Campos obrigatórios: `receivingNote (ManyToOne)`, `stockItem (ManyToOne)`, `sourceLocation (ManyToOne)`, `suggestedLocation (ManyToOne)`, `confirmedLocation (ManyToOne nullable)`, `status (PutawayStatus, default=PENDING)`, `strategyUsed (PutawayStrategy)`, `@Version Long version`

- [ ] Task 3 — Repositório `PutawayTaskRepository` (AC: 8, 10, 11)
  - [ ] 3.1 Criar `PutawayTaskRepository extends JpaRepository<PutawayTask, UUID>` em `com.odin.wms.domain.repository`
  - [ ] 3.2 Método: `existsByTenantIdAndReceivingNoteId(UUID tenantId, UUID receivingNoteId)` — idempotência (AC 8)
  - [ ] 3.3 Método: `findByIdAndTenantId(UUID id, UUID tenantId)` → `Optional<PutawayTask>` (AC 11)
  - [ ] 3.4 Método: `findByTenantIdAndReceivingNoteId(UUID tenantId, UUID receivingNoteId)` → `List<PutawayTask>` (AC 10)
  - [ ] 3.5 Método: `findByTenantId(UUID tenantId, Pageable pageable)` → `List<PutawayTask>` (AC 10)

- [ ] Task 4 — Query de sugestão de localização em `LocationRepository` (AC: 2, 3, 5)
  - [ ] 4.1 Adicionar método JPQL em `LocationRepository`:
        `findStorageOrPickingByWarehouseWithCapacity(UUID warehouseId, UUID tenantId)` — retorna locations `type IN (STORAGE, PICKING)`, `active=true`, pertencentes ao warehouse (via join Shelf→Aisle→Zone→Warehouse), ordenadas por `fullAddress ASC`
  - [ ] 4.2 Adicionar método em `StockItemRepository`:
        `countByTenantIdAndLocationId(UUID tenantId, UUID locationId)` — conta itens em uma location para validar capacidade (AC 5, 9)

- [ ] Task 5 — DTOs (AC: 1, 4, 5, 6, 10)
  - [ ] 5.1 Criar `PutawayTaskResponse` record com: `id, receivingNoteId, stockItemId, productId, productSku, sourceLocationCode, suggestedLocationCode, confirmedLocationCode, status, strategyUsed, createdAt, updatedAt`
  - [ ] 5.2 Criar `ConfirmPutawayRequest` record com: `@NotNull UUID confirmedLocationId`
  - [ ] 5.3 Criar `PutawayTaskSummaryResponse` (para listagem) com subset de campos

- [ ] Task 6 — `PutawayService` — motor de regras e operações (AC: 1–12)
  - [ ] 6.1 Criar `PutawayService` em `com.odin.wms.service` com `@Service @Transactional @RequiredArgsConstructor @Slf4j`
  - [ ] 6.2 Implementar `generateTasks(UUID receivingNoteId, UUID tenantId)`:
        - Buscar ReceivingNote por id+tenantId (404 se não encontrado)
        - Validar status `IN (COMPLETED, COMPLETED_WITH_DIVERGENCE)` → 422 caso contrário (AC 7)
        - Verificar `existsByTenantIdAndReceivingNoteId` → 409 se já gerado (AC 8)
        - Para cada ReceivingNoteItem onde `item.stockItem != null` e `item.stockItem.location.type == RECEIVING_DOCK`:
          - Determinar estratégia: `product.controlsExpiry ? FEFO : FIFO`
          - Chamar `suggestLocation(stockItem, tenantId)` para obter localização sugerida
          - Criar `PutawayTask(status=PENDING, strategyUsed=...)` e salvar
        - Retornar lista de tasks criadas
  - [ ] 6.3 Implementar `suggestLocation(StockItem stockItem, UUID tenantId)`:
        - Buscar locations STORAGE/PICKING do warehouse com capacidade disponível
        - Se FEFO: priorizar locations com mesmo produto tendo lote de menor expiryDate
        - Se FIFO: priorizar locations com mesmo produto (consolidação)
        - Fallback: primeira location com capacidade disponível; null se nenhuma (retornar 422)
        - IMPORTANTE: usar `countByTenantIdAndLocationId` para verificar capacidade quando `capacityUnits != null`
  - [ ] 6.4 Implementar `start(UUID taskId, UUID tenantId)` → `PENDING → IN_PROGRESS` (AC 4); 422 se status != PENDING
  - [ ] 6.5 Implementar `confirm(UUID taskId, ConfirmPutawayRequest request, UUID tenantId)` (AC 5):
        - Buscar task (404 se não encontrada)
        - Validar status `IN (PENDING, IN_PROGRESS)` → 422 com mensagem "Task já confirmada ou cancelada"
        - Buscar `confirmedLocation` e validar: mesmo tenant, mesmo warehouse que source, type IN (STORAGE, PICKING), active=true → 422 caso contrário (AC 9)
        - Validar capacidade: se `capacityUnits != null`, verificar `count < capacityUnits` → 422 se cheio (AC 9)
        - Atualizar `stockItem.location = confirmedLocation` (usa StockItemRepository save)
        - Criar `StockMovement(type=PUTAWAY, sourceLocation=task.sourceLocation, destinationLocation=confirmedLocation, product=product, lot=lot, quantity=stockItem.quantityAvailable, referenceType=PUTAWAY_TASK, referenceId=task.id, operatorId=extractOperatorId())`
        - Criar `AuditLog(action=MOVEMENT, entityType="PutawayTask", entityId=task.id, actorId=..., newValue=JSON com sourceLocation+destination)`
        - Atualizar task: `status=CONFIRMED`, `confirmedLocation=location`
        - Retornar task atualizada
  - [ ] 6.6 Implementar `cancel(UUID taskId, UUID tenantId)` → `PENDING/IN_PROGRESS → CANCELLED` (AC 6); 422 se já CONFIRMED; nenhum movement criado
  - [ ] 6.7 Implementar `findAll(UUID tenantId, PutawayStatus status, UUID warehouseId)` com filtros in-memory para warehouseId (tech debt — mesma limitação de Story 3.1, AC 10)
  - [ ] 6.8 Implementar `findById(UUID taskId, UUID tenantId)` (AC 11)
  - [ ] 6.9 Reutilizar padrão `extractOperatorId()` de `ReceivingNoteService` (SYSTEM_OPERATOR_ID fallback)

- [ ] Task 7 — `PutawayController` (AC: 1, 4, 5, 6, 10, 11, 12)
  - [ ] 7.1 Criar `PutawayController` em `com.odin.wms.controller` com `@RestController @RequestMapping("/api/v1/putaway-tasks") @RequiredArgsConstructor`
  - [ ] 7.2 `POST /api/v1/receiving-notes/{id}/putaway-tasks` → `generateTasks` (201 Created) — `@PreAuthorize("hasAnyRole('WMS_OPERATOR', 'WMS_SUPERVISOR', 'WMS_ADMIN')")`
        **ATENÇÃO:** Este endpoint fica em `ReceivingNoteController` (prefixo /receiving-notes), delegando para `PutawayService`
  - [ ] 7.3 `GET /api/v1/putaway-tasks` → `findAll` com `@RequestParam(required=false) PutawayStatus status, UUID warehouseId` — mesma role
  - [ ] 7.4 `GET /api/v1/putaway-tasks/{id}` → `findById` — mesma role
  - [ ] 7.5 `POST /api/v1/putaway-tasks/{id}/start` → `start` (200) — mesma role
  - [ ] 7.6 `POST /api/v1/putaway-tasks/{id}/confirm` → `confirm` (200) — mesma role
  - [ ] 7.7 `POST /api/v1/putaway-tasks/{id}/cancel` → `cancel` (200) — `@PreAuthorize("hasAnyRole('WMS_SUPERVISOR', 'WMS_ADMIN')")`

- [ ] Task 8 — Testes unitários `PutawayServiceTest` (AC: 1–9)
  - [ ] 8.1 Testar `generateTasks_completedNote_createsTasks` — nota COMPLETED → tasks criadas com estratégia correta
  - [ ] 8.2 Testar `generateTasks_fefoProduct_setsFefoStrategy` — produto controlsExpiry=true → FEFO
  - [ ] 8.3 Testar `generateTasks_fifoProduct_setsFifoStrategy` — produto controlsExpiry=false → FIFO
  - [ ] 8.4 Testar `generateTasks_noteNotCompleted_throws422` — nota PENDING → BusinessException (AC 7)
  - [ ] 8.5 Testar `generateTasks_duplicateCall_throws409` — segunda chamada → ConflictException (AC 8)
  - [ ] 8.6 Testar `confirm_validLocation_movesStockItemAndCreateMovement` — caminho feliz (AC 5)
  - [ ] 8.7 Testar `confirm_locationCapacityExceeded_throws422` — capacidade esgotada (AC 9)
  - [ ] 8.8 Testar `confirm_locationWrongWarehouse_throws422` — cross-warehouse (AC 9)
  - [ ] 8.9 Testar `confirm_alreadyConfirmed_throws422` — task já confirmada
  - [ ] 8.10 Testar `cancel_confirms_remainsInDock` — stockItem permanece na doca (AC 6)

- [ ] Task 9 — Testes de integração `PutawayControllerIntegrationTest` (AC: 1, 5, 8, 10, 11, 12)
  - [ ] 9.1 Setup helper: criar warehouse+zone+aisle+shelf+dock+storage locations, produto, receivingNote COMPLETED com stockItem em dock (reutiliza padrão de `ReceivingNoteControllerIntegrationTest`)
  - [ ] 9.2 Testar `POST /receiving-notes/{id}/putaway-tasks` → 201 com tasks
  - [ ] 9.3 Testar `POST /receiving-notes/{id}/putaway-tasks` segunda vez → 409
  - [ ] 9.4 Testar `POST /putaway-tasks/{id}/confirm` caminho feliz → 200, stockItem.location atualizada, StockMovement criado
  - [ ] 9.5 Testar `GET /putaway-tasks` com filtro por status → retorna lista filtrada
  - [ ] 9.6 Testar `GET /putaway-tasks/{id}` tenant errado → 404 (isolamento multi-tenant, AC 11)
  - [ ] 9.7 Testar `POST /putaway-tasks/{id}/cancel` sem role SUPERVISOR → 403

- [ ] Task 10 — Validação final (AC: todos)
  - [ ] 10.1 Executar `mvn verify` — 0 falhas, 0 erros
  - [ ] 10.2 Verificar cobertura JaCoCo ≥ 80% em `PutawayService` e `PutawayController`
  - [ ] 10.3 Atualizar story: status → `Ready for Review`, todos os checkboxes `[x]`, File List preenchido

---

## Dev Notes

### Contexto de Story Anterior (Story 3.1)

- **Padrão Service/Controller/DTO/Test** estabelecido em Story 3.1: seguir exatamente o mesmo padrão de `ReceivingNoteService` / `ReceivingNoteController`.
- **`extractOperatorId()`**: Método privado em `ReceivingNoteService` que retorna `UUID` do JWT (claim `sub`) ou `SYSTEM_OPERATOR_ID` como fallback. **COPIAR O PADRÃO** — não reinventar.
  ```java
  static final UUID SYSTEM_OPERATOR_ID = UUID.fromString("00000000-0000-0000-0000-000000000001");
  ```
- **Lenient stubbing**: Ao escrever testes com `@ExtendWith(MockitoExtension.class)`, usar `lenient().when(ctx.getAuthentication()).thenReturn(auth)` nos stubs de `@BeforeEach` que não são usados por todos os testes (evita `UnnecessaryStubbingException`).
- **`ReceivingNoteItem.stockItem`**: nullable — só preenchido após `complete()`. Verificar `!= null` antes de gerar putaway task.
- **`ReferenceType`** enum: `PUTAWAY_TASK` precisa ser adicionado (atualizar enum e migration V5).

### Modelo de Dados Existente

**`StockItem`** (`stock_items`):
```java
@ManyToOne Location location;         // localização atual
@ManyToOne ProductWms product;        // produto
@ManyToOne Lot lot;                   // nullable — só se controlsLot=true
int quantityAvailable;
int quantityReserved;
Instant receivedAt;                    // chave FIFO
@Version Long version;                 // optimistic locking
```

**`Location`** (`locations`):
```java
@ManyToOne Shelf shelf;               // Shelf → Aisle → Zone → Warehouse (lazy chain)
String code;
String fullAddress;
LocationType type;                     // STORAGE, PICKING, RECEIVING_DOCK, SHIPPING_DOCK, QUARANTINE, DAMAGED
Integer capacityUnits;                 // null = ilimitado
BigDecimal capacityWeightKg;           // null = ilimitado
Boolean active;
```

**`LocationType`** enum: `STORAGE, PICKING, RECEIVING_DOCK, SHIPPING_DOCK, QUARANTINE, DAMAGED`

**`ProductWms`** (`products_wms`):
```java
Boolean controlsLot;
Boolean controlsSerial;
Boolean controlsExpiry;               // true → FEFO; false → FIFO
StorageType storageType;              // DRY, REFRIGERATED, FROZEN, HAZARDOUS
Integer unitsPerLocation;             // máximo por location (referência de negócio)
```

**`Lot`** (`lots`):
```java
LocalDate expiryDate;                 // chave FEFO — pode ser null se sem validade
String lotNumber;
```

**`MovementType`** enum: inclui `PUTAWAY` já declarado.
**`ReferenceType`** enum: inclui `RECEIVING_NOTE` (adicionado em V4). **Adicionar `PUTAWAY_TASK`** nesta story.
**`AuditAction`** enum: usar `MOVEMENT` para log de putaway.

### Hierarquia de Location para Warehouse

Para verificar se uma location pertence ao mesmo warehouse que a doca:
```java
// Traversal lazy — cuidado com N+1
location.getShelf().getAisle().getZone().getWarehouse().getId()
```
Prefira JPQL join para evitar N+1 (ver `LocationRepository.findReceivingDockByIdAndWarehouse` como exemplo em Story 3.1).

### Algoritmo FIFO/FEFO — Estratégia de Implementação

```
suggestLocation(StockItem item, UUID tenantId):
  1. warehouseId = item.location.shelf.aisle.zone.warehouse.id
  2. locations = locationRepository.findStorageOrPickingByWarehouseWithCapacity(warehouseId, tenantId)
     → type IN (STORAGE, PICKING), active=true, mesmo tenant/warehouse
  3. Para cada location, calcular occupancy:
     occupancy = stockItemRepository.countByTenantIdAndLocationId(tenantId, location.id)
     available = (location.capacityUnits == null) ? true : occupancy < location.capacityUnits
  4. Filter: available == true
  5. if FEFO:
       preferredLocations = locations onde já existe StockItem do mesmo produto,
                            ordenados por MIN(lot.expiryDate) ASC
       return preferredLocations.first() OR any available location
  6. if FIFO:
       preferredLocations = locations onde já existe StockItem do mesmo produto (consolidação)
       return preferredLocations.first() OR any available location
  7. if empty: throw BusinessException("Nenhuma location STORAGE disponível no warehouse")
```

**ATENÇÃO N+1**: A query de `countByTenantIdAndLocationId` para cada location pode gerar N queries. Alternativa: buscar todos os counts em uma query só (GROUP BY location_id) — implementação sugerida:
```java
@Query("SELECT s.location.id, COUNT(s) FROM StockItem s WHERE s.tenantId = :tenantId GROUP BY s.location.id")
List<Object[]> countByTenantIdGroupByLocationId(@Param("tenantId") UUID tenantId);
```

### Validações de `confirm()` — Checklist

```
1. task existe e pertence ao tenant → 404 se não
2. task.status IN (PENDING, IN_PROGRESS) → 422 se CONFIRMED/CANCELLED
3. confirmedLocation existe e pertence ao tenant → 404 se não
4. confirmedLocation.type IN (STORAGE, PICKING) → 422 se dock, quarantine, etc.
5. confirmedLocation.active == true → 422 se inativa
6. confirmedLocation.shelf.aisle.zone.warehouse == task.sourceLocation.shelf.aisle.zone.warehouse → 422 se cross-warehouse
7. capacityUnits: se não null → count < capacityUnits → 422 se cheio
```

### Excepções Existentes

Reutilizar as classes já existentes:
- `BusinessException` — para 422 Unprocessable Entity
- `ConflictException` — para 409 Conflict (duplicata de putaway)
- `ResourceNotFoundException` — para 404 Not Found

### Arquivo de Migração

Criar `V5__putaway_tasks.sql` em:
```
services/wms-service/src/main/resources/db/migration/V5__putaway_tasks.sql
```

### Estrutura de Arquivos Novos (Convenção Estabelecida)

```
services/wms-service/src/main/java/com/odin/wms/
├── domain/
│   ├── entity/
│   │   └── PutawayTask.java
│   ├── enums/
│   │   ├── PutawayStatus.java
│   │   ├── PutawayStrategy.java
│   │   └── ReferenceType.java          (MODIFICAR — adicionar PUTAWAY_TASK)
│   └── repository/
│       └── PutawayTaskRepository.java
├── dto/
│   ├── request/
│   │   └── ConfirmPutawayRequest.java
│   └── response/
│       └── PutawayTaskResponse.java
├── service/
│   └── PutawayService.java
└── controller/
    └── PutawayController.java          (endpoints /api/v1/putaway-tasks)
    └── ReceivingNoteController.java    (MODIFICAR — adicionar POST /{id}/putaway-tasks)

resources/db/migration/
    └── V5__putaway_tasks.sql

test/java/com/odin/wms/
├── putaway/
│   ├── PutawayServiceTest.java         (unit, @ExtendWith(MockitoExtension))
│   └── PutawayControllerIntegrationTest.java (integration, AbstractIntegrationTest)
```

### Testing

**Localização dos testes:**
- Unit tests: `src/test/java/com/odin/wms/putaway/PutawayServiceTest.java`
- Integration tests: `src/test/java/com/odin/wms/putaway/PutawayControllerIntegrationTest.java`

**Padrão de teste unitário** (seguir `ReceivingNoteServiceTest`):
```java
@ExtendWith(MockitoExtension.class)
class PutawayServiceTest {
    @Mock PutawayTaskRepository putawayTaskRepository;
    @Mock StockItemRepository stockItemRepository;
    @Mock LocationRepository locationRepository;
    @Mock StockMovementRepository stockMovementRepository;
    @Mock AuditLogRepository auditLogRepository;
    @Mock ReceivingNoteRepository receivingNoteRepository;
    @InjectMocks PutawayService putawayService;
    // usar lenient().when(...) para stubs de segurança que não são usados por todos os testes
}
```

**Padrão de teste de integração** (seguir `ReceivingNoteControllerIntegrationTest`):
```java
@AutoConfigureMockMvc
class PutawayControllerIntegrationTest extends AbstractIntegrationTest {
    // AbstractIntegrationTest já sobe Testcontainers: PostgreSQL 17, Redis 7, Elasticsearch 8.11.3
    // Usar jwt() com claim "tenant_id" + authority "ROLE_WMS_OPERATOR" para auth
    // Setup: criar warehouse → zone → aisle → shelf → dock location + storage location → produto → receivingNote COMPLETED
}
```

**Frameworks:** JUnit 5, Mockito 5, Spring Boot Test, Testcontainers, Spring Security Test
**Coverage:** JaCoCo ≥ 80% em `PutawayService` (configurado em `pom.xml`)

---

## Change Log

| Data       | Versão | Descrição               | Autor       |
|------------|--------|-------------------------|-------------|
| 2026-02-22 | 1.0.0  | Story criada em Draft   | @sm (River) |

---

## Dev Agent Record

### Agent Model Used

_(a ser preenchido pelo @dev)_

### Debug Log References

_(a ser preenchido pelo @dev)_

### Completion Notes List

_(a ser preenchido pelo @dev)_

### File List

_(a ser preenchido pelo @dev)_

---

## QA Results

_(a ser preenchido pelo @qa após conclusão)_
