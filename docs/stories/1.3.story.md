# Story 1.3: Keycloak Backend Integration

## Status

Done

## Complexity Estimate

5 story points (Small/Medium) — ~3-4h para dev experiente em Spring Security

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["code-review", "integration-test-run"]
```

## Story

**As a** sistema,
**I want** validar tokens JWT do Keycloak e aplicar RBAC por endpoint,
**so that** apenas usuários autenticados com perfil correto acessem os recursos do CRM.

## Acceptance Criteria

1. Spring Security configurado como OAuth2 Resource Server validando JWT do Keycloak existente — `issuer-uri` apontando para realm `odin` no Keycloak compartilhado com o WMS
2. Roles mapeadas: `crm-vendedor`, `crm-gerente`, `crm-sac`, `crm-financeiro`, `crm-admin` — extraídas de `realm_access.roles` no JWT e convertidas para `GrantedAuthority` do Spring Security
3. Endpoint sem token retorna HTTP 401; endpoint com role incorreta retorna HTTP 403
4. `GET /api/v1/me` retorna dados do usuário autenticado: `sub`, `name`, `roles`
5. Realm `odin` e client `odin-crm` existem no Keycloak do WMS — documentar configuração mínima necessária em Dev Notes
6. Testes com `MockMvc` + `SecurityMockMvcRequestPostProcessors.jwt()` validando cenários: sem token (401), role inválida (403), role válida (200), e `/me` retornando payload correto

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual apenas.
> Para habilitar, definir `coderabbit_integration.enabled: true` em core-config.yaml.

## Tasks / Subtasks

- [x] **Task 1: Adicionar `issuer-uri` ao `application.yml` base** (AC: 1)
  - [x] 1.1 Adicionar bloco `spring.security.oauth2.resourceserver.jwt.issuer-uri: ${KEYCLOAK_ISSUER_URI}` em `application.yml` (sem default — obrigatório em prod)
  - [x] 1.2 Confirmar que `application-local.yml` e `application-test.yml` já têm o default `http://localhost:8180/realms/odin` — NÃO alterar esses arquivos

- [x] **Task 2: Adicionar alias `spring-security-test` ao version catalog** (AC: 6)
  - [x] 2.1 Em `gradle/libs.versions.toml`, adicionar na seção `[libraries]`:
    `spring-security-test = { module = "org.springframework.security:spring-security-test" }` (sem version — BOM-managed)
  - [x] 2.2 Confirmar que a versão é gerenciada pelo Spring Boot BOM (não precisa de versão explícita — usar `version.ref = "springBoot"` aponta para BOM-managed)

- [x] **Task 3: Criar `KeycloakJwtAuthenticationConverter`** (AC: 1, 2)
  - [x] 3.1 Criar `crm-web/src/main/java/br/com/odin/crm/web/security/KeycloakJwtAuthenticationConverter.java`
  - [x] 3.2 Implementar `Converter<Jwt, AbstractAuthenticationToken>` extraindo `realm_access.roles` do JWT
  - [x] 3.3 Mapear cada role para `new SimpleGrantedAuthority("ROLE_" + role)` — ex: `crm-vendedor` → `ROLE_crm-vendedor`
  - [x] 3.4 Delegar demais claims (subject, name, email) para `JwtGrantedAuthoritiesConverter` base

- [x] **Task 4: Atualizar `SecurityConfig`** (AC: 1, 2, 3)
  - [x] 4.1 Adicionar `@EnableMethodSecurity` na classe `SecurityConfig`
  - [x] 4.2 Adicionar `.csrf(csrf -> csrf.disable())`
  - [x] 4.3 Adicionar `.sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))`
  - [x] 4.4 Injetar `KeycloakJwtAuthenticationConverter` e configurar `.jwt(jwt -> jwt.jwtAuthenticationConverter(keycloakConverter))`
  - [x] 4.5 Confirmar que `CrmApplicationSmokeTest` ainda passa após as mudanças — `@MockBean JwtDecoder` deve continuar funcionando

- [x] **Task 5: Criar `MeController` e `MeResponse`** (AC: 4)
  - [x] 5.1 Criar record `MeResponse(String sub, String name, List<String> roles)` em `crm-web/.../web/rest/`
  - [x] 5.2 Criar `MeController` com `@RestController @RequestMapping("/api/v1")`
  - [x] 5.3 Implementar `GET /api/v1/me` recebendo `Authentication` do contexto Spring Security; anotar com `@PreAuthorize("hasAnyRole('crm-vendedor','crm-gerente','crm-sac','crm-financeiro','crm-admin')")` — apenas usuários CRM acessam a API (AC3)
  - [x] 5.4 Extrair `sub` e `name` do `JwtAuthenticationToken.getToken().getClaims()` e `roles` das authorities

- [x] **Task 6: Criar `SecurityMvcTest`** (AC: 3, 4, 6)
  - [x] 6.1 Adicionar `testImplementation(libs.spring.security.test)` ao `crm-web/build.gradle.kts`
  - [x] 6.2 Criar `crm-web/src/test/java/br/com/odin/crm/web/security/SecurityMvcTest.java`
  - [x] 6.3 Usar `@WebMvcTest(MeController.class)` + `@Import(SecurityConfig.class)` + `@MockBean KeycloakJwtAuthenticationConverter` + `@MockBean JwtDecoder` + `@ActiveProfiles("test")`
  - [x] 6.4 Teste 1: `GET /api/v1/me` sem token → 401
  - [x] 6.5 Teste 2: `GET /api/v1/me` com JWT contendo role `crm-admin` → 200 com payload correto
  - [x] 6.5b Teste 3: `GET /api/v1/me` com JWT contendo role `ROLE_wms-manager` (não-CRM) → 403 (AC3: role incorreta) — usar `.with(jwt().authorities(new SimpleGrantedAuthority("ROLE_wms-manager")))`
  - [x] 6.6 Teste 4: `GET /actuator/health` sem token → 404 (não 401) — `@WebMvcTest` não carrega actuator, mas 404 confirma que security não bloqueou com 401 (`permitAll()` ativo)
  - [x] 6.7 Usar `SecurityMockMvcRequestPostProcessors.jwt().authorities(new SimpleGrantedAuthority("ROLE_crm-admin"))` para simular JWT

- [x] **Task 7: Verificar `./gradlew test` → BUILD SUCCESSFUL** (AC: 3, 6)
  - [x] 7.1 Executar `./gradlew test` com Docker Desktop rodando
  - [x] 7.2 Confirmar que `CrmApplicationSmokeTest` passa (sem regressão)
  - [x] 7.3 Confirmar que todos os testes de `SecurityMvcTest` passam
  - [x] 7.4 Confirmar que `SchemaIntegrationTest` continua passando (sem regressão)

## Dev Notes

### Contexto e Dependências

Story depende de **Story 1.1 (Done)** e **Story 1.2 (Done)**. Artefatos relevantes:

- `crm-web/src/main/java/.../web/security/SecurityConfig.java` — já tem esqueleto `oauth2ResourceServer` sem converter
- `crm-web/src/main/resources/application-local.yml` — já tem `issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/odin}`
- `crm-web/src/main/resources/application-test.yml` — já tem `issuer-uri` e `org.springframework.security: DEBUG`
- `crm-web/src/test/java/.../CrmApplicationSmokeTest.java` — usa `@MockBean JwtDecoder` — **NÃO alterar**

### `KeycloakJwtAuthenticationConverter` — Código Completo

```java
// Source: architecture.md#Section-10.2 + Section-14.2
package br.com.odin.crm.web.security;

import org.springframework.core.convert.converter.Converter;
import org.springframework.security.authentication.AbstractAuthenticationToken;
import org.springframework.security.core.authority.SimpleGrantedAuthority;
import org.springframework.security.oauth2.jwt.Jwt;
import org.springframework.security.oauth2.server.resource.authentication.JwtAuthenticationToken;
import org.springframework.stereotype.Component;

import java.util.Collection;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

@Component
public class KeycloakJwtAuthenticationConverter implements Converter<Jwt, AbstractAuthenticationToken> {

    @Override
    public AbstractAuthenticationToken convert(Jwt jwt) {
        Collection<SimpleGrantedAuthority> authorities = extractRoles(jwt);
        return new JwtAuthenticationToken(jwt, authorities);
    }

    @SuppressWarnings("unchecked")
    private Collection<SimpleGrantedAuthority> extractRoles(Jwt jwt) {
        Map<String, Object> realmAccess = jwt.getClaim("realm_access");
        if (realmAccess == null) return List.of();
        List<String> roles = (List<String>) realmAccess.getOrDefault("roles", List.of());
        return roles.stream()
                .map(role -> new SimpleGrantedAuthority("ROLE_" + role))
                .collect(Collectors.toList());
    }
}
```

### `SecurityConfig` — Estado Final Após Story 1.3

```java
// Source: architecture.md#Section-10.2
@Configuration
@EnableWebSecurity
@EnableMethodSecurity
public class SecurityConfig {

    private final KeycloakJwtAuthenticationConverter keycloakConverter;

    public SecurityConfig(KeycloakJwtAuthenticationConverter keycloakConverter) {
        this.keycloakConverter = keycloakConverter;
    }

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        return http
            .csrf(csrf -> csrf.disable())
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/actuator/health", "/actuator/health/**", "/actuator/info").permitAll()
                .anyRequest().authenticated())
            .oauth2ResourceServer(oauth2 -> oauth2
                .jwt(jwt -> jwt.jwtAuthenticationConverter(keycloakConverter)))
            .build();
    }
}
```

### `MeController` e `MeResponse`

```java
// MeResponse — Source: PRD#Story-1.3-AC4
public record MeResponse(String sub, String name, List<String> roles) {}

// MeController
@RestController
@RequestMapping("/api/v1")
public class MeController {

    @GetMapping("/me")
    @PreAuthorize("hasAnyRole('crm-vendedor','crm-gerente','crm-sac','crm-financeiro','crm-admin')")
    public ResponseEntity<MeResponse> me(JwtAuthenticationToken authentication) {
        Jwt jwt = authentication.getToken();
        List<String> roles = authentication.getAuthorities().stream()
                .map(a -> a.getAuthority().replace("ROLE_", ""))
                .collect(Collectors.toList());
        return ResponseEntity.ok(new MeResponse(
                jwt.getSubject(),
                jwt.getClaimAsString("name"),
                roles
        ));
    }
}
```

### Configuração Keycloak — Realm `odin` (AC: 5)

O Keycloak do WMS já está rodando em `http://localhost:8180`. Configurar:

1. **Realm:** `odin` (pode já existir para o WMS)
2. **Client:** `odin-crm` com Access Type `bearer-only`
3. **Realm Roles:** criar as 5 roles se não existirem: `crm-vendedor`, `crm-gerente`, `crm-sac`, `crm-financeiro`, `crm-admin`
4. **JWKS endpoint:** `http://localhost:8180/realms/odin/protocol/openid-connect/certs` — utilizado pelo Spring para validar assinatura JWT

Esta story **não** configura o Keycloak automaticamente — é um pré-requisito operacional. Para testes locais, usar tokens mockados via `SecurityMockMvcRequestPostProcessors.jwt()`.

### Discrepância Roles PRD vs Architecture

| PRD (AC2) | Architecture (Section 14.2) | Mapeamento |
|---|---|---|
| `crm-vendedor` | `VENDEDOR` | Keycloak realm usa `crm-` prefix |
| `crm-gerente` | `GERENTE` | idem |
| `crm-sac` | `ATENDIMENTO` | SAC ↔ Atendimento (mesmo perfil) |
| `crm-financeiro` | `FINANCEIRO` | idem |
| `crm-admin` | `ADMIN` | idem |

**Decisão:** seguir o PRD (roles com `crm-` prefix). `@PreAuthorize` usa `hasRole('crm-vendedor')` (Spring remove o `ROLE_` prefix automaticamente).

### Version Catalog — `spring-security-test`

`spring-security-test` é gerenciado pelo Spring Boot BOM. Em `libs.versions.toml`, adicionar sem `version`:

```toml
spring-security-test = { module = "org.springframework.security:spring-security-test" }
```

(A BOM do Spring Boot 3.3.6 gerencia a versão automaticamente — não usar `version.ref`.)

### Estrutura de Arquivos

```
crm-web/
├── src/main/java/br/com/odin/crm/web/
│   ├── security/
│   │   ├── SecurityConfig.java              ← MODIFICAR (Task 4)
│   │   └── KeycloakJwtAuthenticationConverter.java  ← CRIAR (Task 3)
│   └── rest/
│       ├── MeController.java                ← CRIAR (Task 5)
│       └── MeResponse.java                  ← CRIAR (Task 5) — record
├── src/main/resources/
│   └── application.yml                      ← MODIFICAR — add issuer-uri (Task 1)
└── src/test/java/br/com/odin/crm/web/
    └── security/
        └── SecurityMvcTest.java             ← CRIAR (Task 6)

gradle/
└── libs.versions.toml                       ← MODIFICAR — add spring-security-test (Task 2)

crm-web/build.gradle.kts                     ← MODIFICAR — add testImplementation (Task 6.1)
```

### Testing — Padrões

**Framework:** JUnit 5 + MockMvc + `spring-security-test`
**Tipo:** `@WebMvcTest` (slice test — não carrega contexto completo)
**Mock JWT:** `SecurityMockMvcRequestPostProcessors.jwt()`

```java
// Source: architecture.md#Section-15.2
@WebMvcTest(MeController.class)
@Import(SecurityConfig.class)
class SecurityMvcTest {

    @Autowired MockMvc mvc;
    @MockBean KeycloakJwtAuthenticationConverter keycloakConverter;  // se necessário

    @Test
    void withoutToken_returns401() throws Exception {
        mvc.perform(get("/api/v1/me"))
           .andExpect(status().isUnauthorized());
    }

    @Test
    void withValidToken_returns200() throws Exception {
        mvc.perform(get("/api/v1/me")
               .with(jwt().authorities(new SimpleGrantedAuthority("ROLE_crm-admin"))))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.roles[0]").value("crm-admin"));
    }
}
```

**ATENÇÃO:** `@WebMvcTest` não carrega `KeycloakJwtAuthenticationConverter` automaticamente — pode precisar de `@Import` ou `@MockBean`. Testar e ajustar.

## Change Log

| Date       | Version | Description                                                        | Author      |
|------------|---------|--------------------------------------------------------------------|-------------|
| 2026-02-22 | 1.0     | Story criada — baseada em PRD Epic 1 e architecture.md Sections 10.2, 14.2 | River (@sm) |
| 2026-02-22 | 1.1     | Validação @po: GO (9/10). Adicionado `@PreAuthorize` em Task 5.3 + MeController Dev Notes para cobrir AC3 (403). Adicionado subtask 6.5b (teste 403). Status Draft → Ready | Pax (@po) |
| 2026-02-22 | 1.2     | Implementação @dev: Todas as 7 Tasks concluídas. 6 testes passando (CrmApplicationSmokeTest ×2, SecurityMvcTest ×4). BUILD SUCCESSFUL. Status InProgress → InReview | Dex (@dev) |
| 2026-02-22 | 1.3     | QA Gate @qa: PASS. 3 achados low-severity (MNT-001, TEST-001, MNT-002), nenhum bloqueante. Gate file: docs/qa/gates/1.3-keycloak-backend-integration.yml | Quinn (@qa) |
| 2026-02-22 | 1.4     | Push @devops: commit → github.com/ShinyAuroraa/OdinERP branch master. Status: InReview → Done | Gage (@devops) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- **Debug 1 — PropertyPlaceholderHelper / KEYCLOAK_ISSUER_URI**: `@WebMvcTest` carrega `application.yml` base onde `issuer-uri: ${KEYCLOAK_ISSUER_URI}` não tem default. Fix: `@ActiveProfiles("test")` → usa `application-test.yml` que tem o default `http://localhost:8180/realms/odin`.
- **Debug 2 — JwtDecoder em @WebMvcTest**: Após resolver o placeholder, Spring Security tentaria contatar Keycloak para obter OIDC discovery. Fix: `@MockBean JwtDecoder` provê mock e evita contato com Keycloak (mesmo padrão do `CrmApplicationSmokeTest`).

### Completion Notes List

- AC3 (403): Para satisfazer tecnicamente o teste 403, `MeController.me()` recebeu `@PreAuthorize("hasAnyRole('crm-vendedor','crm-gerente','crm-sac','crm-financeiro','crm-admin')")` — decisão de design: apenas usuários CRM acessam a API CRM (corretamente restritivo).
- Task 6.6 (actuator health test): `@WebMvcTest` não carrega actuator endpoints, então o teste espera 404 (não 200). Isso ainda valida o comportamento `permitAll()` — 404 confirma que Security não bloqueou com 401.
- `spring-security-test` adicionado sem `version.ref` ao `libs.versions.toml` — gerenciado pelo Spring Boot BOM 3.3.6 (sem necessidade de versão explícita).
- `CrmApplicationSmokeTest` continua passando sem alterações — `KeycloakJwtAuthenticationConverter` é auto-detectado como `@Component` pelo `scanBasePackages`.
- `SchemaIntegrationTest` (crm-infrastructure) não foi afetado — UP-TO-DATE.

### File List

**Criados:**
- `crm-web/src/main/java/br/com/odin/crm/web/security/KeycloakJwtAuthenticationConverter.java`
- `crm-web/src/main/java/br/com/odin/crm/web/rest/MeController.java`
- `crm-web/src/main/java/br/com/odin/crm/web/rest/MeResponse.java`
- `crm-web/src/test/java/br/com/odin/crm/web/security/SecurityMvcTest.java`

**Modificados:**
- `crm-web/src/main/java/br/com/odin/crm/web/security/SecurityConfig.java`
- `crm-web/src/main/resources/application.yml`
- `crm-web/build.gradle.kts`
- `gradle/libs.versions.toml`

## QA Results

**Reviewer:** Quinn (@qa)
**Date:** 2026-02-22
**Gate:** PASS → `docs/qa/gates/1.3-keycloak-backend-integration.yml`

### Checks

| Check | Result |
|-------|--------|
| Code Review | PASS |
| Unit Tests | PASS |
| Acceptance Criteria | PASS |
| No Regressions | PASS |
| Performance | PASS |
| Security | PASS |
| Documentation | PASS |

### Issues Found

| ID | Severity | Finding |
|----|----------|---------|
| MNT-001 | Low | Unchecked cast `(List<String>)` in `KeycloakJwtAuthenticationConverter.extractRoles()` — ClassCastException possible if JWT `realm_access.roles` is not a List. Risk mitigated by JWKS JWT validation upstream. |
| TEST-001 | Low | `actuatorHealth_withoutToken_isPublic()` expects 404 (not 200) — `@WebMvcTest` doesn't load actuator. 404 correctly confirms `permitAll()` is active (security did not block at 401). Documented in test. |
| MNT-002 | Low | `Collectors.toList()` in `MeController` — `Stream.toList()` (Java 16+, project uses Java 21) is more idiomatic. Cosmetic, optional. |

### Summary

All 6 acceptance criteria met. `./gradlew test` → BUILD SUCCESSFUL (6 tests passing: CrmApplicationSmokeTest ×2, SecurityMvcTest ×4). Implementation is secure (STATELESS, CSRF disabled, RBAC via `@PreAuthorize`), well-tested, and introduces no regressions. Three low-severity non-blocking findings documented above.
